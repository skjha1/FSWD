```js

// // Function declation
// function myFunction(param1, param2) {
//     // Function body 
//     // you can perform operations using param1 and param2 
// }

// // Function Invocation

// myFunction(10, 20);

// // Return statement

// function add(x, y) {
//     let result = x + y;
//     return result;
//     // Function body 
//     // you can perform operations using param1 and param2 
// }

// // Scope 


// let globalVariable = 10;

// function myFunction1() {
//     let localVariable = 20;
//     console.log(globalVariable); // Accessing global variable
//     console.log(localVariable); // Accessing local variable
// }

// console.log(globalVariable); // Accessing global variable outside of the fxn 
// console.log(localVariable); // This will throw an error as localvariable is not accessible outside of the fxn


// function add(x, y) {
//     return x + y;
// }

// function findMax(a, b) {
//     return a > b ? a : b;
// }

// function isEven(num) {
//     return num % 2 === 0; 
// }


// let num1 = 10;
// let num2 = 20;

// // Calling the add function
// let sum = add(num1, num2);
// console.log(`Sum of ${num1} and ${num2} is: ${sum}`);

// // Calling the findMax function
// let max = findMax(num1, num2);
// console.log(`Maximum of ${num1} and ${num2} is: ${max}`);

// // Calling the isEven function
// let checkNum = 15;
// console.log(`${checkNum} is even? ${isEven(checkNum)}`);



// var myArray = []; // Empty Array
// var myNumbers = [1, 2, 3, 4, 5]; // Array with numbers
// var myString = ["apple", "banana", "Orange"]; // Array with String

// myArray.push(6); // Add 6 to the end of the array
// console.log(myArray)
// var lastElement = myArray.pop();  // Removes and returns the last element of the array
// console.log(lastElement)
// var firstElemt = myArray.shift(); // Remove and return the 1st element of the Array

// // Push pop shift unshift --  splice slice

// var fruits = ["apple", "banana", "orange"];
// fruits.splice(1, 1, "grapes"); // Remove 1 elememt at the index 1 and adds "Grapes" in its place
// var citrus = fruits.slice(1,3); // Returns a new Array containing "banana" and "orange"



// var myObject = {}; // Empty Object

// var person = {
//     name: "Shiva",
//     age: 25,
//     city: "Patna"
// }; // Objets with properties

// person.name = "Shiva"; // Adding a property to an object
// person.age = 25; // Adding another property 

// person.sayHello = function() {
//     console.log("Hello, my name is " + this.name);
// }; // Adding a method to the object

// var name = person.name; // Accessing property using dot notation
// var age = person['age']; // Accessing property using square bracket notation
// person['city'] = "Chennai"; // Adding or modifying property using square bracket notion
// person.sayHello(); // Invoking method using dot notation


// Select the button element elemt 
// const button = document.querySelector('#myButton'); 

// // Add an event listeners to the button
// button.addEventListener('click', function() {
//     // Code to execute when the button is clicked;
//     console.log("Button Clicked")
// });
```
